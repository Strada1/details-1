{
  "dependencies": {
    "date-fns": "^2.29.3"
  },
  "devDependencies": {
    "parcel": "^2.7.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html"
  }
}
